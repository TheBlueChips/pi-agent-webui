# dependencies and runtime artifacts (all auto-installed/downloaded on launch)
bridge/node_modules/
bridge/whisper/
pi-mobile/node_modules/
pi-mobile/dist/
pi-mobile/windows/
uploads/
workspace/

# machine-specific pi agent source choice (native vs docker container name)
bridge/agent-source.txt

# local UI preferences (may contain an uploaded avatar)
webui-settings.json

# logs
*.log
